name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build -t bharathichethana/fsdcicd:${{ github.sha }} .
          docker tag bharathichethana/fsdcicd:${{ github.sha }} bharathichethana/fsdcicd:latest

      - name: Push Docker Images
        run: |
          docker push bharathichethana/fsdcicd:${{ github.sha }}
          docker push bharathichethana/fsdcicd:latest

  deploy:
    needs: build-and-push
    runs-on: self-hosted

    steps:
      - name: Pull Latest Docker Image
        run: |
          sudo docker pull bharathichethana/fsdcicd:latest

      - name: Stop Existing Container
        run: |
         sudo docker stop aws-cicd-container || true
         sudo docker rm aws-cicd-container || true

      - name: Run New Container
        run: |
          sudo docker run -d \
            --name aws-cicd-container \
            -p 80:80 \
            --restart always \
            bharathichethana/fsdcicd:latest
